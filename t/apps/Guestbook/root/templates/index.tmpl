<html>
<head><title>OX - Guestbook Example</title>
<body>
<form method="POST">
    <input type='text' name='note' />
    [% action({
       type    => 'button',
       bind_to => { resource => 'guestbook' },
       value   => 'Add New Post'
    }) %]
</form>
<hr/>
<div id="posts">[%-
    outlet({
        type     => 'list.text',
        bind_to  => 'model/posts',
        template => '<div class="post">%s</div>'
    })
-%]</div>
</body>
</html>